{
  "version": 1,
  "maxIterations": 5,
  "completionMarker": "LOOP_COMPLETE",
  "initialPlanPrompt": "Survey the codebase, then write `.localagent-box/loop-plan.md` with milestones, iteration order, and how to verify. Planning only — no implementation. Later harness cycles should follow this file.\n\nGoal: {{goal}}",
  "steps": [
    {
      "verb": "OBSERVE",
      "prompt": "Inspect only the files relevant to the next unfinished item in `.localagent-box/loop-plan.md` (when present), then state the smallest next change toward the goal. Do not implement yet.\nIf the goal is already fully achieved, output a line exactly: `{{completionMarker}}: true` and nothing else.\n\nGoal: {{goal}}\nIteration: {{iteration}}"
    },
    {
      "verb": "ACT",
      "prompt": "Implement the change identified in the previous step. Edit repository files and run focused checks.\n\nGoal: {{goal}}\nIteration: {{iteration}}"
    },
    {
      "verb": "REFLECT",
      "prompt": "Evaluate progress toward the goal. If the goal is fully achieved, output a line exactly: `{{completionMarker}}: true`\nOtherwise summarize remaining gaps.\n\nGoal: {{goal}}\nIteration: {{iteration}}"
    }
  ]
}
